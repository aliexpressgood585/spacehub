# Format: one line, "MODE [MONTHS]".
#
# v92bt: the combination. Owner coin selection (worth +0.033R gross, v91bt)
# moved to 4h where the 0.16% round trip is ~5% of the stop instead of 39%,
# plus the isolated-leverage ceiling: where liquidation comes inside the stop.
v92bt 6
