# Push a change to this file to run a backtest without the GitHub connector.
# Format: one line, "MODE [MONTHS]". Lines starting with # are ignored.
# The mode must also be in the fetch-step whitelist in backtest.yml, or it will
# silently get the short-history fetcher (see the v78bt note in CLAUDE.md).
#
# v81bt: THE LADDER DIFF. Five 36-month runs failed to explain why the sim will
# not reproduce +0.0469R, and both my hypotheses were refuted by measurement —
# the second in the opposite direction from my prediction. So: stop guessing.
# Put the v79bt inline ladder and shared/strategy.ts side by side on IDENTICAL
# trades, gross and net, and find where they disagree. No portfolio, no capital.
v81bt 36
