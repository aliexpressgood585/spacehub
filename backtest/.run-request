# Format: one line, "MODE [MONTHS]".
#
# v90bt: the fast regime-switching engine the owner asked for — 15m bars,
# breakout when ADX is high, band-fade when ADX is low, tight stop, quick target.
# Reports GROSS vs NET side by side, because every previous fast-trading result
# here died on that comparison and it is the only question that matters.
v90bt 6
