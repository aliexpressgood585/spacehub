# Format: one line, "MODE [MONTHS]".
#
# v88bt: the aggression surface, owner-requested. Two dials — base risk, which
# the 95% heat cap absorbs past a point, and LEVERAGE, which is what "high risk"
# actually means in this engine. Reports RUIN (windows at -90% or worse), not
# just return, because an average over paths that ended at zero is a lie.
v88bt 36
