# Format: one line, "MODE [MONTHS]".
#
# v87bt: the missing cell. v86bt measured ROTA-only at +29.3% / maxDD 12.3% with
# five of six windows positive — the best configuration ever measured here — but
# did NOT run it at 6bps, and v71bt is the standing reminder that a config can
# look excellent at 3bps and die on execution cost. Also carries a PROPERLY
# designed path-perturbation probe, because v86bt's starting-cash probe tested a
# scale axis the engine is invariant to and therefore measured nothing.
v87bt 36
