# Format: one line, "MODE [MONTHS]".
#
# v93bt: the owner actual account — the deployed config at \$500 with ISOLATED
# leverage (margin per position, liquidation at maintenance margin, account
# death). DONCH4H cannot trade at \$500 because MIN_NOTIONAL is an absolute \$500,
# so ROTA is the only sleeve that can size anything.
v93bt 12
