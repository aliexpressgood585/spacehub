# Format: one line, "MODE [MONTHS]".
#
# v89bt: leverage for real — margin accounting, isolated liquidation at
# maintenance margin, and account death. v88bt's leverage rows were identical
# because the engine was a cash account that could not borrow. Built with the
# liquidation engine attached on purpose: a leverage model without one reports
# profits earned by a dead account.
v89bt 36
