# Push a change to this file to run a backtest without the GitHub connector.
# Format: one line, "MODE [MONTHS]". Lines starting with # are ignored.
# The mode must also be in the fetch-step whitelist in backtest.yml, or it will
# silently get the short-history fetcher (see the v78bt note in CLAUDE.md).
#
# last run: v80bt 36 (2026-09-19) — FIRST capital-constrained portfolio run.
# Reads: what the deployed config does with a real $10,000; whether ROTA starves
# DONCH4H; what the caps cost and which cap; what pyramid units 2 and 3 add;
# whether the allocation policy matters; slippage + maker-fill sensitivity.
v80bt 36
